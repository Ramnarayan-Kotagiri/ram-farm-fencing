import type { SideId } from './farmSurvey';
export const contractorLengths: Record<SideId, number> = { W: 800, N: 1621.39, E: 450, S: 1750 };
// North receives a usable length only for an explicit future-work override.
